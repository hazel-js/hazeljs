# Observability

Organism events use `organism.*` namespaces via `OrganismEventEmitter` (separate from `AgentEventType`).

Metrics: `OrganismMetrics` with optional OpenTelemetry soft-bind (`hazel.organism.active_agents`, etc.).

`debug: true` prints structured decision summaries — never private chain-of-thought.
