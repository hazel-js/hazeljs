# Constitution

Mission-level rules that bind every agent. Enforcement compiles to `PolicyEngine` rules and runtime asserts (PII, refund limits, budget).

Children may inherit fewer permissions; never a superset.
