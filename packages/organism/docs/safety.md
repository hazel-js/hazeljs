# Safety

Hard limits: max agents, generation depth, children, spawn rate, hourly cost.

Also: capability reuse, constitution checks, kill switch / emergency stop, approval hooks, minimum age/samples before survival kill.
